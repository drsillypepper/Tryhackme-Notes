It's a router's job to connect networks and pass data between them. It does this by using routing (hence the name router!).
- routing involves creating paths between networks so that the data could be delivered(some complex math involved)
- routing is useful when there are many paths to the destination, or the paths are somewhat dynamic.
	