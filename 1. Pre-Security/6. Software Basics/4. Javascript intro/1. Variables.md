```javascript
let tries = 0;
let guess = 0;
```

Javascript is dynamically typed language so there are no specific keywords for different kinds of variables.


# Constants
Constants are the types of variables that cannot be changed after initialization

example of const
```javascript
const secret = Math.floor(Math.random() * (20)) + 1;
```